/*
 * CreateCard Messages
 *
 * This contains all the text for the CreateCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CreateCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Create Card!',
  },
});
